# Backend mise configuration
# Inherits from root .mise.toml and adds backend-specific settings

[tools]
node = "24.21.0"
fnox = "latest"

[env]
# Backend development settings
NODE_ENV = "development"
