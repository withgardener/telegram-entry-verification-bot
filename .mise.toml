# tg-watchdog mise configuration
# This file defines the development environment tools

[tools]
# Both services target the supported Node.js 24 LTS line.
node = "24.21.0"
fnox = "latest"

[env]
# Default environment for development
NODE_ENV = "development"
